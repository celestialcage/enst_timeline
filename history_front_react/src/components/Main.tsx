// import { Outlet } from "react-router-dom";

import TurnList from "./TurnList";

const mainClassName = ``;

const Main = () => {
  return (
    <div className={mainClassName}>
      {/* <Outlet /> */}
      <TurnList />
    </div>
  );
};

export default Main;
